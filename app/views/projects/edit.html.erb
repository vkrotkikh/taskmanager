<div class="popup-container">

<h2>Edit project</h2>

<%= simple_form_for @project do |f| %>
    <p>Current Name: "<strong><%= @project.name %> </strong>"<br></p>
    <%= f.input :name %>
    <div class="clearfix">
    	<%= f.button :submit, class:"pull-right btn-info"  %>
    </div>
<% end %>
<div class="links-row">
	<%= link_to '← Back to Projects', projects_path %>
</div>
</div>