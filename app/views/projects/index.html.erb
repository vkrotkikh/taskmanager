<% @projects.each do |i| %>
    <p><%= link_to i.name, project_path(i) %></p>
    <p><%= link_to i.id, project_path(i) %></p>
    <p><%= link_to i.user_id, project_path(i) %></p></br>

<% end %>

<p>
<%= link_to 'Click here to create new project', new_project_path %>
</p>

